---
description: 核对当前上下文观察值，保存后按需压缩并继续
---

1. 使用当前客户端、精确会话的可用原生指标。用户偏好、模型规格、累计或缓存用量不能代替当前占用。无可靠指标就说明 UNKNOWN。
2. 实际观察达到80%时，增量保存当前角色/Lane的重要决定、失败、限制和下一步，减少下一批读取；不自动Bye、不新开对话。
3. 客户端压缩完成后重新核对当前Lane检查点和必要证据，再继续原任务；不沿用压缩前百分比或全量Boot/Resume。
4. 客户端确实无法继续、压缩失败未恢复、关键状态矛盾未解决或用户主动交接时，先保存已确认内容并明确缺口。普通保存不是完整Bye，压缩不保证记忆无损。
